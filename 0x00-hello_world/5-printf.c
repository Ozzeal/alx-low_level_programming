#include <stdio.h>
/**
 * main - writing a program that prints a line using the printf
 * Return: 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
